# YAW SWEEP — 정반 2. GENERATED by
# scripts/generate_calibration_artifacts.py (lift 0 mm, view 0.50 m)
# — do not hand-edit; re-run the generator.
#
# THIS IS NOT A CALIBRATION. One path tag (141), one ref tag
# (3), 6 different CAMERA YAWS spanning 355°. The base never
# moves, so front_cam's view of the tag is identical every time
# and cancels; only the ARM side varies.
#
# Read it with: rosrun path_tag_locator analyse_yaw_sweep.py <session_dir>
#   large circle radius          -> HAND-EYE is wrong
#   radius ~0 but the normal session shows a large absolute
#   error                        -> FRONT_CAM (T_mb2fc) is wrong
#
# ⚠️ Park the base ON tag 141 and leave it there. Do NOT reorder
# the entries and do NOT let a TASK/GOTO move the base midway —
# a moved base breaks the common factor the whole method rests
# on. Run with ref_tags_path = config/reference_tags_plate2.yaml.

defaults:
  retry_count: 1
  align_required: true

plan:
  - path_tag_id: 141    # E y +0.650 | YAW SWEEP 1/6 | cam yaw 0° | flange 1.155 m
    ref_tag_id: 3
    yaw_sweep_deg: 0   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [338.5, 952.7, 85.1, 180.0, 0.4, 88.5]
    # START HERE — park the base on this tag and do not move it
  - path_tag_id: 141    # E y +0.650 | YAW SWEEP 2/6 | cam yaw 45° | flange 0.739 m
    ref_tag_id: 3
    yaw_sweep_deg: 45   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [212.4, 730.5, 85.1, 180.0, 0.4, 43.5]
  - path_tag_id: 141    # E y +0.650 | YAW SWEEP 3/6 | cam yaw 85° | flange 0.513 m
    ref_tag_id: 3
    yaw_sweep_deg: 85   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [-4.8, 660.2, 85.1, 180.0, 0.4, 3.5]
  - path_tag_id: 141    # E y +0.650 | YAW SWEEP 4/6 | cam yaw 130° | flange 0.764 m
    ref_tag_id: 3
    yaw_sweep_deg: 130   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [-237.2, 766.5, 85.1, 180.0, 0.4, -41.5]
  - path_tag_id: 141    # E y +0.650 | YAW SWEEP 5/6 | cam yaw 170° | flange 1.136 m
    ref_tag_id: 3
    yaw_sweep_deg: 170   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [-326.1, 976.7, 85.1, 180.0, 0.4, -81.5]
  - path_tag_id: 141    # E y +0.650 | YAW SWEEP 6/6 | cam yaw 355° | flange 1.197 m
    ref_tag_id: 3
    yaw_sweep_deg: 355   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [340.8, 981.7, 85.1, 180.0, 0.4, 93.5]
