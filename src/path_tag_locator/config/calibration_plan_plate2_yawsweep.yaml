# YAW SWEEP — 정반 2. GENERATED by
# scripts/generate_calibration_artifacts.py (lift 0 mm, view 0.50 m)
# — do not hand-edit; re-run the generator.
#
# THIS IS NOT A CALIBRATION. One path tag (141), one ref tag
# (3), 6 different CAMERA YAWS spanning 355°. The base never
# moves, so front_cam's view of the tag is identical every time
# and cancels; only the ARM side varies.
#
# Read it with: rosrun path_tag_locator analyse_yaw_sweep.py <session_dir>
#   large circle radius          -> HAND-EYE is wrong
#   radius ~0 but the normal session shows a large absolute
#   error                        -> FRONT_CAM (T_mb2fc) is wrong
#
# ⚠️ Park the base ON tag 141 and leave it there. Do NOT reorder
# the entries and do NOT let a TASK/GOTO move the base midway —
# a moved base breaks the common factor the whole method rests
# on. Run with ref_tags_path = config/reference_tags_plate2.yaml.

defaults:
  retry_count: 1
  align_required: true

plan:
  - path_tag_id: 141    # E y +0.650 | YAW SWEEP 1/6 | cam yaw 0° | flange 1.070 m
    ref_tag_id: 3
    yaw_sweep_deg: 0   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [-155.4, 937.3, 60.2, 178.7, 0.3, -92.7]
    # START HERE — park the base on this tag and do not move it
  - path_tag_id: 141    # E y +0.650 | YAW SWEEP 2/6 | cam yaw 145° | flange 1.341 m
    ref_tag_id: 3
    yaw_sweep_deg: 145   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [91.3, 1140.5, 60.2, 178.7, 0.3, 122.3]
  - path_tag_id: 141    # E y +0.650 | YAW SWEEP 3/6 | cam yaw 195° | flange 1.069 m
    ref_tag_id: 3
    yaw_sweep_deg: 195   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [166.3, 1020.3, 60.2, 178.7, 0.3, 72.3]
  - path_tag_id: 141    # E y +0.650 | YAW SWEEP 4/6 | cam yaw 250° | flange 0.727 m
    ref_tag_id: 3
    yaw_sweep_deg: 250   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [112.0, 875.5, 60.2, 178.7, 0.3, 17.3]
  - path_tag_id: 141    # E y +0.650 | YAW SWEEP 5/6 | cam yaw 300° | flange 0.706 m
    ref_tag_id: 3
    yaw_sweep_deg: 300   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [-23.4, 834.1, 60.2, 178.7, 0.3, -32.7]
  - path_tag_id: 141    # E y +0.650 | YAW SWEEP 6/6 | cam yaw 355° | flange 1.037 m
    ref_tag_id: 3
    yaw_sweep_deg: 355   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [-149.3, 924.0, 60.2, 178.7, 0.3, -87.7]
