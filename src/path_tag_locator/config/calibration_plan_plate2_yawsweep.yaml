# YAW SWEEP — 정반 2. GENERATED by
# scripts/generate_calibration_artifacts.py (lift 0 mm, view 0.50 m)
# — do not hand-edit; re-run the generator.
#
# THIS IS NOT A CALIBRATION. One path tag (141), one ref tag
# (3), 6 different CAMERA YAWS spanning 355°. The base never
# moves, so front_cam's view of the tag is identical every time
# and cancels; only the ARM side varies.
#
# Read it with: rosrun path_tag_locator analyse_yaw_sweep.py <session_dir>
#   large circle radius          -> HAND-EYE is wrong
#   radius ~0 but the normal session shows a large absolute
#   error                        -> FRONT_CAM (T_mb2fc) is wrong
#
# ⚠️ Park the base ON tag 141 and leave it there. Do NOT reorder
# the entries and do NOT let a TASK/GOTO move the base midway —
# a moved base breaks the common factor the whole method rests
# on. Run with ref_tags_path = config/reference_tags_plate2.yaml.

defaults:
  retry_count: 1
  align_required: true

plan:
  - path_tag_id: 141    # E y +0.650 | YAW SWEEP 1/6 | cam yaw 0° | flange 1.113 m
    ref_tag_id: 3
    yaw_sweep_deg: 0   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [-165.2, 973.7, 85.1, -179.5, -0.7, -91.6]
    # START HERE — park the base on this tag and do not move it
  - path_tag_id: 141    # E y +0.650 | YAW SWEEP 2/6 | cam yaw 150° | flange 1.311 m
    ref_tag_id: 3
    yaw_sweep_deg: 150   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [129.9, 1105.4, 85.1, -179.5, -0.7, 118.4]
  - path_tag_id: 141    # E y +0.650 | YAW SWEEP 3/6 | cam yaw 200° | flange 1.008 m
    ref_tag_id: 3
    yaw_sweep_deg: 200   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [164.2, 968.2, 85.1, -179.5, -0.7, 68.4]
  - path_tag_id: 141    # E y +0.650 | YAW SWEEP 4/6 | cam yaw 255° | flange 0.685 m
    ref_tag_id: 3
    yaw_sweep_deg: 255   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [68.2, 847.2, 85.1, -179.5, -0.7, 13.4]
  - path_tag_id: 141    # E y +0.650 | YAW SWEEP 5/6 | cam yaw 305° | flange 0.752 m
    ref_tag_id: 3
    yaw_sweep_deg: 305   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [-73.2, 849.6, 85.1, -179.5, -0.7, -36.6]
  - path_tag_id: 141    # E y +0.650 | YAW SWEEP 6/6 | cam yaw 355° | flange 1.080 m
    ref_tag_id: 3
    yaw_sweep_deg: 355   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [-162.3, 959.4, 85.1, -179.5, -0.7, -86.6]
