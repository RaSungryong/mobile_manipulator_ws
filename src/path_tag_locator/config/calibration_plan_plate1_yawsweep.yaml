# YAW SWEEP — 정반 1. GENERATED by
# scripts/generate_calibration_artifacts.py (lift 0 mm, view 0.50 m)
# — do not hand-edit; re-run the generator.
#
# THIS IS NOT A CALIBRATION. One path tag (103), one ref tag
# (0), 6 different CAMERA YAWS spanning 355°. The base never
# moves, so front_cam's view of the tag is identical every time
# and cancels; only the ARM side varies.
#
# Read it with: rosrun path_tag_locator analyse_yaw_sweep.py <session_dir>
#   large circle radius          -> HAND-EYE is wrong
#   radius ~0 but the normal session shows a large absolute
#   error                        -> FRONT_CAM (T_mb2fc) is wrong
#
# ⚠️ Park the base ON tag 103 and leave it there. Do NOT reorder
# the entries and do NOT let a TASK/GOTO move the base midway —
# a moved base breaks the common factor the whole method rests
# on. Run with ref_tags_path = config/reference_tags.yaml.

defaults:
  retry_count: 1
  align_required: true

plan:
  - path_tag_id: 103    # B y -0.650 | YAW SWEEP 1/6 | cam yaw 0° | flange 1.229 m
    ref_tag_id: 0
    yaw_sweep_deg: 0   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [-323.8, 1044.9, 85.1, 180.0, 0.4, -91.5]
    # START HERE — park the base on this tag and do not move it
  - path_tag_id: 103    # B y -0.650 | YAW SWEEP 2/6 | cam yaw 185° | flange 1.119 m
    ref_tag_id: 0
    yaw_sweep_deg: 185   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [333.8, 933.9, 85.1, 180.0, 0.4, 83.5]
  - path_tag_id: 103    # B y -0.650 | YAW SWEEP 3/6 | cam yaw 225° | flange 0.746 m
    ref_tag_id: 0
    yaw_sweep_deg: 225   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [212.5, 740.5, 85.1, 180.0, 0.4, 43.5]
  - path_tag_id: 103    # B y -0.650 | YAW SWEEP 4/6 | cam yaw 270° | flange 0.523 m
    ref_tag_id: 0
    yaw_sweep_deg: 270   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [-33.7, 672.5, 85.1, 180.0, 0.4, -1.5]
  - path_tag_id: 103    # B y -0.650 | YAW SWEEP 5/6 | cam yaw 310° | flange 0.772 m
    ref_tag_id: 0
    yaw_sweep_deg: 310   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [-237.0, 776.5, 85.1, 180.0, 0.4, -41.5]
  - path_tag_id: 103    # B y -0.650 | YAW SWEEP 6/6 | cam yaw 355° | flange 1.187 m
    ref_tag_id: 0
    yaw_sweep_deg: 355   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [-326.2, 1015.9, 85.1, 180.0, 0.4, -86.5]
