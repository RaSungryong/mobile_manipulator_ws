# YAW SWEEP — 정반 1. GENERATED by
# scripts/generate_calibration_artifacts.py (lift 0 mm, view 0.50 m)
# — do not hand-edit; re-run the generator.
#
# THIS IS NOT A CALIBRATION. One path tag (103), one ref tag
# (0), 6 different CAMERA YAWS spanning 355°. The base never
# moves, so front_cam's view of the tag is identical every time
# and cancels; only the ARM side varies.
#
# Read it with: rosrun path_tag_locator analyse_yaw_sweep.py <session_dir>
#   large circle radius          -> HAND-EYE is wrong
#   radius ~0 but the normal session shows a large absolute
#   error                        -> FRONT_CAM (T_mb2fc) is wrong
#
# ⚠️ Park the base ON tag 103 and leave it there. Do NOT reorder
# the entries and do NOT let a TASK/GOTO move the base midway —
# a moved base breaks the common factor the whole method rests
# on. Run with ref_tags_path = config/reference_tags.yaml.

defaults:
  retry_count: 1
  align_required: true

plan:
  - path_tag_id: 103    # B y -0.650 | YAW SWEEP 1/6 | cam yaw 0° | flange 1.151 m
    ref_tag_id: 0
    yaw_sweep_deg: 0   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [165.2, 1036.3, 85.1, -179.5, -0.7, 88.4]
    # START HERE — park the base on this tag and do not move it
  - path_tag_id: 103    # B y -0.650 | YAW SWEEP 2/6 | cam yaw 50° | flange 0.815 m
    ref_tag_id: 0
    yaw_sweep_deg: 50   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [126.3, 900.4, 85.1, -179.5, -0.7, 38.4]
  - path_tag_id: 103    # B y -0.650 | YAW SWEEP 3/6 | cam yaw 100° | flange 0.671 m
    ref_tag_id: 0
    yaw_sweep_deg: 100   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [-2.8, 842.7, 85.1, -179.5, -0.7, -11.6]
  - path_tag_id: 103    # B y -0.650 | YAW SWEEP 4/6 | cam yaw 155° | flange 0.950 m
    ref_tag_id: 0
    yaw_sweep_deg: 155   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [-138.6, 916.3, 85.1, -179.5, -0.7, -66.6]
  - path_tag_id: 103    # B y -0.650 | YAW SWEEP 5/6 | cam yaw 205° | flange 1.273 m
    ref_tag_id: 0
    yaw_sweep_deg: 205   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [-160.8, 1056.0, 85.1, -179.5, -0.7, -116.6]
  - path_tag_id: 103    # B y -0.650 | YAW SWEEP 6/6 | cam yaw 355° | flange 1.183 m
    ref_tag_id: 0
    yaw_sweep_deg: 355   # analysis marker; the orchestrator ignores it
    arm_view_tcp_mm_deg: [162.3, 1050.6, 85.1, -179.5, -0.7, 93.4]
